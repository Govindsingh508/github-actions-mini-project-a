name: Mini CI/CD Pipeline

on:
  workflow_dispatch:

jobs:
  build:

    name: Build Application

    runs-on: ubuntu-latest
    
    steps:
    
      - name: checkout code
      
        uses: actions/checkout@v4
